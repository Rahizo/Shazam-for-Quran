# Quran Corpus

Run `npm run download:quran` to create `quran-corpus.json`.

The matching corpus should use verified Quran text verbatim. The downloader is structured for Quran.com / Quran.Foundation metadata and keeps this directory ready for a Tanzil-backed corpus export when available.
